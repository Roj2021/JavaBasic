package class2;

public class DecimalNumbers {
    public static void main(String[] args) {
        /*
        if we want store decimal numbers(10.5 110.5 1.6) in computer's memory using java we can use following
         float
         decimal
         */
        float myNumberBox=10.5f;
        float mySecondBox=5.5f;
        double largedecimalBox=134545455.12121;
        System.out.println(myNumberBox);
        /*
        if we want to store character (#,$,o,s,4) we can use char datatype
         */

        char letter='s';
        char gender='m';
        char symbol='m';
        char numberChar='1';
        /*
        String data type belongs to a different category called non-primitives that's why for String rules are
        a little different for example S of is upper case its color is also not blue we will discuss these
        details later on.
         */

        String name="Sam";

        boolean hungry=true;// true means yes
        boolean sleepy=false;// false mean no
    }
}
